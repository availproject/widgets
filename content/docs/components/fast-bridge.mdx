---
title: Fast Bridge
description: Bridge assets across chains with source breakdown, fee details, progress steps, and allowance flow. Intent-based and optimized for UX.
component: true
deprecated: true
deprecationMessage: Fast Bridge is deprecated in favor of Nexus One. For new integrations, use Nexus One for the unified swap, send, and deposit experience.
links:
  doc: https://docs.availproject.org/nexus/avail-nexus-sdk/bridge-methods/bridge
  api: https://docs.availproject.org/nexus/avail-nexus-sdk/api-reference#bridge-operations
---

## Preview

<ComponentPreview
  name="fast-bridge"
  description="An intent-based fast bridge with allowance flow and progress."
  align="center"
/>

## Build with Nexus Elements Skills

<span className="block mt-6" />

Nexus Elements ships with agent skills that help you set up providers, integrate components, and ship faster.

Install the skill pack once in your environment:

```bash
npx skills add availproject/nexus-elements
```
<span className="block mt-6" />

For more information view https://skills.sh/availproject/nexus-elements.

<span className="block mt-6" />

Recommended skills for this component:

- `nexus-elements-overview`
- `nexus-elements-nexus-provider`
- `nexus-elements-fast-bridge`
- `nexus-elements-unified-balance`
- `nexus-elements-view-history`
- `nexus-elements-common`

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash
    npx shadcn@latest add @nexus-elements/fast-bridge
    ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      
      <Step>Install these dependencies</Step>
```bash
npm install @avail-project/nexus-core @radix-ui/react-accordion @radix-ui/react-dialog @radix-ui/react-label @radix-ui/react-select @radix-ui/react-slot class-variance-authority clsx lucide-react tailwind-merge
```
<Step>Copy and paste the following code into your project.</Step>
      <div
        data-slot="code"
        className="my-4 overflow-hidden **:data-rehype-pretty-code-figure:m-0! **:data-rehype-pretty-code-figure:rounded-t-none **:data-rehype-pretty-code-figure:border-t [&_pre]:max-h-[400px]"
      >
        <ComponentSource name="fast-bridge" showAllFiles />
      </div>
      <Step>Update the import paths to match your project setup.</Step>

    </Steps>

  </TabsContent>
</CodeTabs>

## Usage

<Step className="font-serif text-lg">
  Don't forget to wrap your app with NexusProvider
</Step>
<Step className="font-serif text-base">
  View Detailed instructions{" "}
  <a href="/docs/get-started" target="_blank" className="underline">
    here
  </a>
</Step>

```tsx showLineNumbers
<FastBridge
  connectedAddress={address}
  onComplete={completeCurrent}
  prefill={{
    token: "USDC",
    chainId: SUPPORTED_CHAINS.BASE,
  }}
  onError={handleError}
  onStart={handleStart}
/>
```

## API Reference

### FastBridge

The `FastBridge` component uses the `bridge` function from `@avail-project/nexus-core`. It can be fully customized and styled.

<CodeCollapsibleWrapper className="mt-4">

```ts title="FastBridgeProps"
interface FastBridgeProps {
  connectedAddress: Address; // connected address to the wallet
  maxAmount?: string | number; // optional cap for bridge amount (for campaign-specific limits)
  prefill?: {
    token: SUPPORTED_TOKENS; // token to bridge
    chainId: SUPPORTED_CHAINS_IDS; // chain to bridge to
    amount?: string; // amount to bridge
    recipient?: Address; // recipient address to receive the bridged assets
  };
  onComplete?: () => void; // callback when the bridge is complete
  onStart?: () => void; // callback when the bridge starts
  onError?: (message: string) => void; // callback when an error occurs
}
```

</CodeCollapsibleWrapper>
