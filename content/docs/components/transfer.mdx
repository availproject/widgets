---
title: Fast Transfer
description: Transfer assets across chains with source breakdown, fee details, progress steps, and allowance flow. Intent-based and optimized for UX.
component: true
deprecated: true
deprecationMessage: Fast Transfer is deprecated in favor of Nexus One. For new integrations, use NexusOne with config mode send.
links:
  doc: https://docs.availproject.org/nexus/avail-nexus-sdk/bridge-methods/transfer
  api: https://docs.availproject.org/nexus/avail-nexus-sdk/api-reference#transfer-operations
---

## Preview

<ComponentPreview
  name="fast-transfer"
  description="An intent-based fast transfer with allowance flow and progress."
  align="center"
/>

## Build with Nexus Elements Skills

<span className="block mt-6" />

Nexus Elements ships with agent skills that help you set up providers, integrate components, and ship faster.

Install the skill pack once in your environment:

```bash
npx skills add availproject/nexus-elements
```
<span className="block mt-6" />

For more information view https://skills.sh/availproject/nexus-elements.

<span className="block mt-6" />

Recommended skills for this component:

- `nexus-elements-overview`
- `nexus-elements-nexus-provider`
- `nexus-elements-transfer`
- `nexus-elements-unified-balance`
- `nexus-elements-view-history`
- `nexus-elements-common`

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash
    npx shadcn@latest add @nexus-elements/fast-transfer
    ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      
      <Step>Install these dependencies</Step>
```bash
npm install @avail-project/nexus-core @radix-ui/react-accordion @radix-ui/react-dialog @radix-ui/react-label @radix-ui/react-select @radix-ui/react-slot class-variance-authority clsx lucide-react tailwind-merge
```
<Step>Copy and paste the following code into your project.</Step>
      <div
        data-slot="code"
        className="my-4 overflow-hidden **:data-rehype-pretty-code-figure:m-0! **:data-rehype-pretty-code-figure:rounded-t-none **:data-rehype-pretty-code-figure:border-t [&_pre]:max-h-[400px]"
      >
        <ComponentSource name="fast-transfer" showAllFiles />
      </div>
      <Step>Update the import paths to match your project setup.</Step>

    </Steps>

  </TabsContent>
</CodeTabs>

## Usage

<Step className="font-serif text-lg">
  Don't forget to wrap your app with NexusProvider
</Step>
<Step className="font-serif text-base">
  View Detailed instructions{" "}
  <a href="/docs/get-started" target="_blank" className="underline">
    here
  </a>
</Step>

```tsx showLineNumbers
<FastTransfer
  onComplete={completeCurrent}
  prefill={{
    token: "USDC",
    chainId: SUPPORTED_CHAINS.BASE,
  }}
  onError={handleError}
  onStart={handleStart}
/>
```

## API Reference

### FastTransfer

The `FastTransfer` component uses the `transfer` function from `@avail-project/nexus-core`. It can be fully customized and styled.

<CodeCollapsibleWrapper className="mt-4">

```ts title="FastTransferProps"
interface FastTransferProps {
  maxAmount?: string | number; // optional cap for transfer amount
  prefill?: {
    token: SUPPORTED_TOKENS; // token to transfer
    chainId: SUPPORTED_CHAINS_IDS; // destination chain
    amount?: string; // amount to transfer
    recipient?: Address; // recipient address to receive the transferred assets
  };
  onComplete?: () => void; // callback when the transfer is complete
  onStart?: () => void; // callback when the transfer starts
  onError?: (message: string) => void; // callback when an error occurs
}
```

</CodeCollapsibleWrapper>
